{
  "name": "shos-med-functions",
  "description": "Cloud Functions for SHOS Med — Stripe checkout + webhooks",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^4.9.0",
    "stripe": "^16.12.0"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  }
}
